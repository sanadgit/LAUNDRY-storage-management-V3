# n8n Live Integration Readiness

Generated by `npm run audit:n8n-live`.

## Executive Summary

- Existing backend routes scanned: 171.
- Required n8n service contracts checked: 35.
- Ready exact: 18.
- Needs adapter/alias: 8.
- Missing: 9.

The n8n workflow package is validated, and the backend now exposes part of the `/api/v1` contract surface. Installing Meta/POS tokens alone is not enough until the remaining contracts pass staging tests.

## What Is Already Present

- WhatsApp webhook verification and inbound processing exist at `/api/webhooks/whatsapp`.
- AI router exists at `/api/ai/router`.
- Local AI operations tables and service methods exist for conversations, pickups, complaints, driver assignments, and WhatsApp logging.
- POS lookup routes exist under `/api/pos/...`, including order search and order details.
- Customer portal order and tracking routes exist under `/api/customer/...`.

## What Blocks Live n8n

- 17 required n8n contracts still need exact backend support or deeper adapter work.
- 8 routes have legacy behavior available but still need v1-safe envelopes, ownership checks, or status mapping.
- 9 routes are still missing entirely.
- Customer identity by phone has a v1 adapter, but POS customer-master behavior must still be verified in staging with safe duplicate/ambiguous handling.
- Production Meta/POS tokens should wait until remaining pickup, driver, complaint, handoff, retry, and dead-letter contracts are implemented or intentionally disabled.

## Contract Matrix

| Group | Workflow | Method | Required endpoint | Readiness | Existing/legacy route | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Webhook Idempotency | 02-message-idempotency-check | GET | `/api/v1/webhook-events/:messageId` | Ready exact | `GET /api/v1/webhook-events/:param` | Required to check duplicate inbound WhatsApp wamid before side effects. |
| Webhook Idempotency | 02-message-idempotency-check | POST | `/api/v1/webhook-events/lock` | Ready exact | `POST /api/v1/webhook-events/lock` | Required to reserve processing lock for exactly-once behavior. |
| Webhook Idempotency | 01-whatsapp-customer-service-router | POST | `/api/v1/webhook-events/processed` | Ready exact | `POST /api/v1/webhook-events/processed` | Router currently marks processing through /api/ai/router style behavior, not a dedicated event state route. |
| Customer Identity | 04-pos-customer-identity, 06-ai-customer-service-agent | GET | `/api/v1/customers/by-phone/:phone` | Ready exact | `GET /api/v1/customers/by-phone/:param` | Needs POS-backed customer lookup and safe ambiguous-match handling. |
| Customer Orders | 06-ai-customer-service-agent, 10-order-tracking | GET | `/api/v1/customers/:customerId/orders/active` | Ready exact | `GET /api/v1/customers/:param/orders/active` | Customer portal route exists but requires customer/admin auth and does not match n8n service-token contract. |
| POS Orders | 06-ai-customer-service-agent, 10-order-tracking, 40-complaint-management, 50-human-handoff | GET | `/api/v1/pos/orders/:orderId/status` | Ready exact | `GET /api/v1/pos/orders/:param/status` | POS lookup exists, but n8n needs a stable service-token endpoint that verifies phone/customer ownership. |
| Conversation Memory | 05-conversation-memory-manager | GET | `/api/v1/ai/conversations/active` | Ready exact | `GET /api/v1/ai/conversations/active` | Conversation list exists for staff; n8n needs active conversation lookup by customer/message context. |
| Conversation Memory | 05-conversation-memory-manager | POST | `/api/v1/ai/conversations` | Ready exact | `POST /api/v1/ai/conversations` | Needed when no active conversation exists. |
| Conversation Memory | 05-conversation-memory-manager | GET | `/api/v1/ai/conversations/:conversationId/messages/recent` | Ready exact | `GET /api/v1/ai/conversations/:param/messages/recent` | Legacy route exists with staff auth and different response envelope. |
| Conversation Memory | 05-conversation-memory-manager | GET | `/api/v1/ai/conversations/:conversationId/summary` | Ready exact | `GET /api/v1/ai/conversations/:param/summary` | conversation_summaries table exists but dedicated API route is not exposed. |
| Conversation Memory | 05-conversation-memory-manager, 50-human-handoff, 51-human-reply-return, 80-save-conversation-summary | PATCH | `/api/v1/ai/conversations/:conversationId` | Ready exact | `PATCH /api/v1/ai/conversations/:param` | Legacy route exists with staff auth; n8n needs service-token auth and v1 envelope. |
| AI Agent | 06-ai-customer-service-agent | GET | `/api/v1/ai/customer-service-agent/prompt` | Ready exact | `GET /api/v1/ai/customer-service-agent/prompt` | Prompt can be file/static for now, but workflow expects a service API route. |
| AI Agent | 40-complaint-management | POST | `/api/v1/ai/complaints/classify` | Missing |  | Current AI service classifies complaint-like intents, but no complaint classifier API exists. |
| Knowledge Base | 06-ai-customer-service-agent | GET | `/api/v1/knowledge-base/search` | Ready exact | `GET /api/v1/knowledge-base/search` | No knowledge base route exposed yet. |
| Pickup | 20-create-pickup-request | GET | `/api/v1/pickups/duplicate` | Missing |  | Required to prevent duplicate pickup creation. |
| Pickup | 20-create-pickup-request | POST | `/api/v1/pickups` | Needs adapter/alias | `POST /api/pickups` | Pickup creation exists but route/envelope do not match v1 n8n contract. |
| Pickup | 30-driver-dispatch | GET | `/api/v1/pickups/:pickupId/dispatch-details` | Needs adapter/alias | `GET /api/pickups` | Need a single safe dispatch-details route. |
| Routing | 21-uae-area-branch-resolver | GET | `/api/v1/service-areas/aliases` | Missing |  | Service areas exist in AI database config, but no public v1 alias route exists. |
| Routing | 21-uae-area-branch-resolver | GET | `/api/v1/service-areas/resolve-coordinates` | Missing |  | Needed for coordinate-to-area matching. |
| Routing | 20-create-pickup-request, 21-uae-area-branch-resolver | GET | `/api/v1/branches/resolve` | Needs adapter/alias | `GET /api/branches` | Branch list exists behind staff auth; n8n needs service-area-aware resolver. |
| Driver Dispatch | 30-driver-dispatch | GET | `/api/v1/drivers/available` | Missing |  | Driver ranking exists inside aiOperationsService, but no v1 available-drivers API route exists. |
| Driver Dispatch | 30-driver-dispatch, 33-driver-timeout-reassignment | POST | `/api/v1/dispatch/assignments` | Needs adapter/alias | `POST /api/pickups/:id/assign-driver` | Assignment exists for pickup id, but n8n contract expects generic dispatch assignments. |
| Driver Dispatch | 31-driver-whatsapp-notification | GET | `/api/v1/driver-assignments/:assignmentId/safe-notification-data` | Missing |  | Required to avoid exposing unsafe customer/driver data in notifications. |
| Driver Dispatch | 32-driver-status-update, 33-driver-timeout-reassignment | PATCH | `/api/v1/driver-assignments/:assignmentId/status` | Needs adapter/alias | `PATCH /api/driver-assignments/:id/status` | Status update exists but uses old route and status enum shape. |
| Complaints | 40-complaint-management | POST | `/api/v1/complaints` | Needs adapter/alias | `POST /api/complaints` | Complaint creation exists but v1 contract must preserve originalComplaintWording and ownership checks. |
| Complaints | 41-notify-branch-manager, 43-close-complaint, 50-human-handoff | GET | `/api/v1/complaints/:complaintId` | Needs adapter/alias | `GET /api/complaints` | List route exists; single complaint route and branch-safe envelope needed. |
| Complaints | 43-close-complaint | PATCH | `/api/v1/complaints/:complaintId/status` | Needs adapter/alias | `PATCH /api/complaints/:id` | Legacy update exists; critical closure rules need v1 enforcement. |
| Handoff | 50-human-handoff | POST | `/api/v1/human-handoffs` | Missing |  | human_escalations table exists; dedicated handoff route is not exposed. |
| Handoff | 50-human-handoff | PATCH | `/api/v1/ai/conversations/:conversationId/lock` | Ready exact | `PATCH /api/v1/ai/conversations/:param/lock` | Conversation update exists but explicit AI lock route is missing. |
| Notifications | 31-driver-whatsapp-notification, 41-notify-branch-manager, 70-whatsapp-response-sender | POST | `/api/v1/notifications/whatsapp` | Ready exact | `POST /api/v1/notifications/whatsapp` | WhatsApp send exists but notification log/idempotency envelope route is missing. |
| Conversation Save | 51-human-reply-return, 80-save-conversation-summary | POST | `/api/v1/ai/messages` | Ready exact | `POST /api/v1/ai/messages` | AI messages are saved internally, but no direct service-token route exists. |
| Conversation Save | 80-save-conversation-summary | POST | `/api/v1/ai/tool-calls/batch` | Ready exact | `POST /api/v1/ai/tool-calls/batch` | ai_tool_calls table exists; batch logging route is missing. |
| Observability | 90-central-error-handler | POST | `/api/v1/observability/errors` | Ready exact | `POST /api/v1/observability/errors` | Needed for central n8n error handler. |
| Retry | 91-retry-queue | GET | `/api/v1/retry-records/:retryRecordId` | Missing |  | Retry queue storage routes are missing. |
| Retry | 91-retry-queue | POST | `/api/v1/dead-letter` | Missing |  | Dead-letter queue API is missing. |

## Recommended Next Build Order

1. Configure a staging-only `SERVICE_API_TOKEN` and import the MVP workflows into n8n staging.
2. Run staging API tests for duplicate `wamid`, phone mismatch, POS unavailable, and WhatsApp send idempotency.
3. Implement the remaining pickup, driver, complaint, handoff, retry, and dead-letter v1 adapters.
4. Configure `N8N_WF_*` workflow IDs after importing sub-workflows.
5. Configure production Meta/OpenAI/POS secrets only after staging API tests pass.

## Token Readiness Answer

Do not add production Meta/POS tokens yet. Staging tokens can be configured after deploying the current `/api/v1` adapter layer and setting `SERVICE_API_TOKEN`; production tokens should wait until the remaining contracts and staging tests pass.

