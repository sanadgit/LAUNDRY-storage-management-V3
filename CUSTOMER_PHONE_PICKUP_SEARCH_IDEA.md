# فكرة بحث الطلبات برقم الهاتف وتجهيز الاستلام

## الهدف

إنشاء صفحة أو ميزة بحث ذكية تساعد العامل يبحث برقم هاتف الزبون، ثم تعرض كل الطلبات المرتبطة بهذا الرقم من النظام، مع توضيح حالة كل طلب ومكان تخزين الأصناف حتى يصل العامل للطلب بسرعة.

الفكرة الأساسية: العامل يدخل رقم الهاتف، النظام يجلب الطلبات المفتوحة أو القابلة للتجهيز، ويقسم محتويات الفاتورة حسب نوع الصنف، ثم يعطي زر `Pick` لكل مجموعة أصناف لفتح نافذة توضّح مكانها.

## البحث برقم الهاتف

### مدخل البحث

- حقل إدخال رقم الهاتف.
- زر بحث.
- دعم البحث حتى لو الرقم مكتوب بصيغ مختلفة إن أمكن لاحقا:
  - `0501234567`
  - `971501234567`
  - `+971501234567`

### نتيجة البحث

يعرض النظام كل الطلبات المرتبطة برقم الهاتف، لأن الزبون قد يكون لديه أكثر من طلب في نفس الوقت.

لكل طلب يجب عرض:

- رقم الهاتف.
- رقم الطلب.
- اسم الزبون.
- حالة الطلب.
- `Remark` القادم من النظام.
- زر فتح الفاتورة.
- أقسام الأصناف الموجودة داخل الفاتورة.

## حالات الطلبات المطلوبة

النظام يعرض الطلبات التي حالتها واحدة من الحالات التالية:

1. `Packed Partially`
2. `Fully packed`
3. `pending`
4. `pending/Unpaid`

## حالة الطلب إذا كان Delivered

إذا كان الطلب حالته `Delivered` لا يظهر كطلب قابل للـ Pick، لكن يجب توضيح رسالة للعامل مثل:

> هذا الطلب تم توصيله بتاريخ: `Delivery Date`

ويتم عرض تاريخ التوصيل إن كان موجودا في بيانات الطلب.

## شكل بطاقة الطلب

كل طلب يظهر في بطاقة واضحة تحتوي على:

| الحقل | الوصف |
| --- | --- |
| Phone | رقم هاتف الزبون |
| Order No | رقم الطلب |
| Customer Name | اسم الزبون |
| Status | حالة الطلب |
| Remark | الملاحظة القادمة من النظام |
| Invoice | زر فتح الفاتورة |

إذا كان الطلب يحتوي على أكثر من نوع صنف داخل الفاتورة، يتم تقسيمه إلى مجموعات:

1. `Clothes`
2. `Home Items (Phase 2)`
3. `Blankets (Phase 3)`

كل مجموعة تظهر كسطر أو كارت صغير، وبجانبها زر:

- `Pick`

عند الضغط على `Pick` يتم فتح نافذة `Popup / Modal` تعرض مكان التخزين بطريقة مبسطة.

## تقسيم الأصناف داخل الفاتورة

يجب قراءة أصناف الفاتورة وتصنيفها حسب نوعها:

### 1. Clothes

أصناف الملابس العادية.

مكان التخزين الخاص بها يأتي من `Remark` الموجود مع بيانات الطلب.

أمثلة من النظام:

```text
Pack: 75,
Pack: A
Pack: B
Pack: C
Pack: D
Pack: Up
```

القيم المقبولة:

- رقم من `1` إلى `300`.
- أو حرف/كود من:
  - `A`
  - `B`
  - `C`
  - `D`
  - `Up`

في نافذة الـ Pick يتم عرض:

- عنوان كبير: `Clothes`
- مكان التخزين:
  - `Pack: 75`
  - أو `Pack: A`
  - أو القيمة الموجودة في الـ Remark.

### 2. Home Items (Phase 2)

أصناف المفروشات أو الـ Home Items.

مكان التخزين الخاص بها أيضا يأتي من `Remark` الموجود مع بيانات الطلب.

مثال Remark قادم من النظام:

```text
,Fb4,75 :Pack
```

هنا يمكن أن يحتوي الـ Remark على أكثر من مكان:

- `75` خاص بـ Clothes.
- `Fb4` خاص بـ Home Items.

### قاعدة قراءة كود Home Items

الكود مثل:

```text
Fb4
```

يتم تفسيره كالتالي:

| الجزء | المعنى |
| --- | --- |
| `F` | اسم الاستور |
| `b` | العمود أو الحرف داخل الاستور |
| `4` | رقم الصف أو الخانة |

> ملاحظة مهمة: يجب تثبيت التسمية في الواجهة بشكل موحد. المثال المطلوب من التشغيل يقول: `Store: F`, `Column = 1`, `Row = A` عند الكود `fa1`. لذلك يمكن اعتماد القراءة العملية التالية:
>
> - الحرف الأول = Store
> - الحرف الثاني = Row
> - الرقم = Column

### Store F

الاستور `F` مكون من:

- Rows / صفوف:
  - `A`
  - `B`
  - `C`
  - `D`
  - `E`
  - `F`
- Columns / عواميد:
  - `1`
  - `2`
  - `3`
  - `4`
  - `5`
  - `6`

أمثلة:

```text
Pack: Fa1
Pack: Fb4
```

مثال عرض في نافذة الـ Pick للكود `Fa1`:

```text
Store: F
Row: A
Column: 1
```

مثال عرض للكود `Fb4`:

```text
Store: F
Row: B
Column: 4
```

### Store Bs

الاستور `Bs` مكون من:

- Rows / صفوف:
  - `A`
  - `B`
- Columns / عواميد:
  - `1`
  - `2`
  - `3`
  - `4`
  - `5`

أمثلة من النظام:

```text
Pack: Bsa1
Pack: Bsb1
```

مثال عرض للكود `Bsa1`:

```text
Store: Bs
Row: A
Column: 1
```

مثال عرض للكود `Bsb1`:

```text
Store: Bs
Row: B
Column: 1
```

## 3. Blankets (Phase 3)

أصناف البطاطين لا تعتمد على `Remark` القادم من النظام.

مكان التخزين الخاص بها هو المكان المحفوظ داخل نظام الاستور الذي تم بناؤه للبطاطين.

عند الضغط على `Pick` لمجموعة `Blankets (Phase 3)`:

- يقرأ النظام رقم الطلب أو رقم الفاتورة.
- يبحث داخل قاعدة بيانات تخزين البطاطين.
- يعرض مكان البطاطين المحفوظ سابقا.

مثال عرض في النافذة:

```text
Blankets Storage
Store: [اسم الاستور المحفوظ]
Location: [الخانة أو المكان المحفوظ]
Quantity: [العدد إن وجد]
```

## نافذة Pick Popup

عند الضغط على زر `Pick` لأي مجموعة، تفتح نافذة واضحة وبسيطة للعامل.

### محتوى النافذة

- اسم نوع الصنف كعنوان كبير:
  - `Clothes`
  - `Home Items`
  - `Blankets`
- رقم الطلب.
- اسم الزبون.
- مكان التخزين.
- زر فتح الفاتورة.
- زر إغلاق.

### طريقة عرض Home Items

إذا كانت المجموعة `Home Items (Phase 2)` يتم عرض المكان بصيغة كبيرة وسهلة:

```text
Store: F
Row: B
Column: 4
```

أو:

```text
Store: Bs
Row: A
Column: 1
```

### طريقة عرض Clothes

إذا كانت المجموعة `Clothes` يتم عرض:

```text
Pack: 75
```

أو:

```text
Pack: A
```

### طريقة عرض Blankets

إذا كانت المجموعة `Blankets (Phase 3)` يتم عرض المكان القادم من نظام تخزين البطاطين، وليس من Remark النظام الخارجي.

## قواعد Parsing للـ Remark

### مثال مركب

Remark من النظام:

```text
,Fb4,75 :Pack
```

المطلوب:

- استخراج `Fb4` لمجموعة `Home Items`.
- استخراج `75` لمجموعة `Clothes`.

### قواعد مقترحة

1. تنظيف النص من:
   - `Pack:`
   - `:Pack`
   - المسافات الزائدة.
   - الفواصل الزائدة في البداية أو النهاية.
2. تقسيم النص باستخدام الفاصلة `,`.
3. كل جزء يتم تحليله:
   - إذا كان رقم من `1` إلى `300` فهو غالبا `Clothes`.
   - إذا كان `A/B/C/D/Up` فهو `Clothes`.
   - إذا بدأ بـ `F` ويتبعه حرف ثم رقم، فهو `Home Items - Store F`.
   - إذا بدأ بـ `Bs` ويتبعه حرف ثم رقم، فهو `Home Items - Store Bs`.
4. إذا لم يتم فهم جزء من الـ Remark، يتم عرضه في خانة `Unrecognized Remark` حتى لا تضيع المعلومة على العامل.

## حالات عدم وجود بيانات

### لا توجد طلبات

رسالة مقترحة:

```text
No orders found for this phone number.
```

### توجد طلبات لكن كلها Delivered

يتم عرض الطلبات مع رسالة لكل طلب:

```text
Delivered on: [Delivery Date]
```

### يوجد طلب قابل للـ Pick لكن لا يوجد Remark

رسالة مقترحة:

```text
No storage remark found for this order.
```

### Blankets بدون مكان محفوظ

رسالة مقترحة:

```text
No blanket storage location found for this order.
```

## اقتراح اسم الميزة

أسماء مناسبة داخل النظام:

- `Phone Pickup Search`
- `Customer Order Picker`
- `Smart Pickup Finder`
- `بحث الطلبات برقم الهاتف`

## ملخص سير العمل

1. العامل يدخل رقم الهاتف.
2. النظام يجلب كل طلبات الزبون من النظام الخارجي.
3. النظام يفلتر الحالات القابلة للتجهيز:
   - `Packed Partially`
   - `Fully packed`
   - `pending`
   - `pending/Unpaid`
4. إذا الطلب `Delivered` يتم عرض تاريخ التوصيل فقط.
5. كل طلب يظهر مع بياناته الأساسية وزر فتح الفاتورة.
6. النظام يقرأ أصناف الفاتورة ويقسمها:
   - Clothes
   - Home Items
   - Blankets
7. لكل مجموعة يظهر زر `Pick`.
8. زر `Pick` يفتح نافذة تعرض مكان التخزين:
   - Clothes من `Remark`.
   - Home Items من `Remark` بعد تفسير كود الاستور.
   - Blankets من نظام تخزين البطاطين الداخلي.

## نقاط تحتاج تأكيد قبل التنفيذ

1. هل `Column` و `Row` في Home Items يجب عرضها كما في المثال:

```text
Store: F
Column = 1
Row = A
```

أم الأفضل توحيدها كالتالي؟

```text
Store: F
Row: A
Column: 1
```

2. ما أسماء الأصناف أو التصنيفات الدقيقة داخل الفاتورة التي تميز:
   - Clothes
   - Home Items
   - Blankets

3. ما الحقل الرسمي لتاريخ التوصيل في بيانات الطلب عندما تكون الحالة `Delivered`؟

4. هل زر فتح الفاتورة يفتح:
   - رابط الفاتورة في النظام الخارجي؟
   - أو نافذة داخلية داخل هذا النظام؟
