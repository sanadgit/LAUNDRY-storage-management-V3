# خطة تنبيهات أخطاء التسليم حسب رقم هاتف العميل

## 1) ملخص الفكرة
المطلوب إضافة نظام تنبيهات ذكي داخل صفحة **تنبيه العملاء / Alerts** يربط الطلبات برقم هاتف العميل، وليس برقم الطلب فقط.

الفكرة الأساسية:
- إذا كان العميل لديه أكثر من طلب بنفس رقم الهاتف، يظهر تنبيه واضح قبل التسليم أو عند مراجعة الطلب.
- إذا كان POS يعتبر الطلب `Delivered` لكن ما زالت توجد قطع/بطانيات لنفس الطلب داخل الاستور، يظهر تنبيه خطر.
- إذا كانت كمية POS لا تطابق كمية القطع الموجودة في الاستور، يظهر تنبيه نقص أو زيادة.
- إذا كان الكاشير سلّم جزء من الطلب ونسي قطعة أو بطانية، يساعد النظام على كشف ذلك من رقم الهاتف وأرقام الطلبات المرتبطة به.

الهدف ليس إرسال رسالة للعميل مباشرة عند وجود خطأ، بل تنبيه الموظف داخلياً أولاً حتى لا يتم تسليم ناقص أو ترك طلب قديم منسي.

---

## 2) المشكلة التشغيلية
في التشغيل اليومي تحصل حالات مثل:
- العميل لديه أكثر من طلب بنفس رقم الهاتف.
- الموظف يرى طلباً واحداً فقط ويسلمه، بينما يوجد طلب آخر لنفس العميل ما زال في الاستور.
- POS يظهر أن الطلب تم تسليمه `Delivered`، لكن قطعة أو قطعتين ما زالتا موجودتين في التخزين.
- كمية الطلب في POS مختلفة عن عدد القطع المخزنة في النظام.
- رقم الطلب يكون صحيحاً لكن رقم الهاتف أو اسم العميل يكشف أن للعميل طلبات أخرى يجب مراجعتها.

هذه الحالات يصعب كشفها إذا كان النظام يفكر برقم الطلب فقط. لذلك نحتاج طبقة كشف تعتمد على رقم الهاتف.

---

## 3) مكانها داخل النظام
لا نحتاج صفحة منفصلة. الأفضل تطوير صفحة التنبيهات الحالية:

`/customer-alerts`

بإضافة Tabs:
1. **مطابقة الطلبات**
   - التاب الحالي: رقم الطلب، كمية POS، كمية الاستور، إرسال واتساب.

2. **تنبيهات رقم الهاتف**
   - التاب الجديد.
   - يجمع كل الطلبات المرتبطة بنفس رقم الهاتف.
   - يظهر حالات الخطر قبل التسليم.

3. **تم التسليم وما زال في الاستور**
   - فلتر سريع أو تاب خاص للحالات الأخطر.
   - يعرض الطلبات التي حالتها في POS هي `Delivered` لكن لها مواقع تخزين داخل النظام.

4. **سجل المعالجة**
   - الحالات التي تم حلها أو تجاهلها أو تأجيلها.

---

## 4) أنواع التنبيهات المقترحة

### A) العميل لديه أكثر من طلب
الشرط:
- نفس رقم الهاتف مرتبط بأكثر من رقم طلب.
- واحد أو أكثر من هذه الطلبات موجود حالياً في الاستور.

مثال التنبيه:
```text
هذا العميل لديه 3 طلبات مرتبطة بنفس رقم الهاتف. راجع كل الطلبات قبل التسليم.
```

الخطورة:
- `Medium` إذا كل الطلبات غير مسلمة.
- `High` إذا أحد الطلبات مكتمل وطلب آخر ناقص أو قديم.

### B) الطلب Delivered لكن ما زال موجوداً في الاستور
الشرط:
- حالة الطلب في POS = `Delivered`.
- يوجد لنفس رقم الطلب قطع داخل جدول التخزين.

مثال التنبيه:
```text
الطلب 256719 حالته Delivered في POS، لكن توجد 1 قطعة ما زالت في الاستور.
```

الخطورة:
- `Critical`

هذا أهم تنبيه لأنه يكشف احتمال تسليم ناقص أو خطأ تسجيل.

### C) كمية POS لا تطابق كمية الاستور
الشرط:
- `qty_in_store < qty_in_order`: ناقص.
- `qty_in_store > qty_in_order`: زائد.

مثال:
```text
الطلب 256719 ناقص: POS = 4 قطع، الاستور = 3 قطع.
```

الخطورة:
- `High` إذا ناقص.
- `Medium` إذا زائد.

### D) طلب قديم لنفس العميل
الشرط:
- نفس رقم الهاتف لديه طلب مخزن منذ مدة طويلة.
- مثال: أكثر من 7 أو 14 يوم حسب سياسة التشغيل.

مثال:
```text
يوجد طلب قديم لنفس العميل منذ 12 يوم. تأكد قبل تسليم الطلب الحالي.
```

الخطورة:
- `Medium`

### E) بيانات هاتف ناقصة أو متغيرة
الشرط:
- POS لا يرجع رقم هاتف.
- أو يوجد أكثر من صيغة رقم لنفس العميل.

مثال:
```text
لا يوجد رقم هاتف واضح لهذا الطلب، لا يمكن ربطه تلقائياً بطلبات العميل الأخرى.
```

الخطورة:
- `Low` أو `Medium` حسب الحالة.

---

## 5) طريقة الكشف المقترحة

### الخطوة 1: قراءة الاستور
قراءة كل أرقام الطلبات المخزنة حالياً من جدول التخزين:
- `blankets.blanket_number`
- `store`
- `row`
- `column`
- `status`
- `created_at`

### الخطوة 2: جلب بيانات POS لكل طلب
لكل رقم طلب:
- رقم الطلب.
- رقم الهاتف.
- اسم العميل.
- تاريخ الطلب.
- تاريخ التوصيل.
- العنوان.
- remark.
- السعر.
- حالة الطلب:
  - `Delivered`
  - `Fully Packed`
  - `Partially Packed`
  - `Pending`
  - `Pending/Unpaid`

### الخطوة 3: تنظيف رقم الهاتف
توحيد الصيغة قبل التجميع:
- إزالة المسافات.
- إزالة `+`.
- التعامل مع أرقام تبدأ بـ `0`.
- الاحتفاظ بصيغة normalized مثل `9715xxxxxxx` أو الصيغة التي تناسب بياناتكم.

### الخطوة 4: التجميع حسب رقم الهاتف
إنشاء مجموعة لكل رقم هاتف:
```ts
phone_group = {
  phone,
  customer_names,
  order_numbers,
  stored_orders,
  delivered_but_stored_orders,
  mismatch_orders,
  oldest_stored_at,
  severity,
  alerts
}
```

### الخطوة 5: حساب الخطورة
ترتيب الحالات:
- `Critical`: طلب Delivered لكن ما زال له قطع في الاستور.
- `High`: نقص كمية أو أكثر من طلب لنفس العميل مع طلب ناقص.
- `Medium`: أكثر من طلب لنفس العميل أو طلب قديم.
- `Low`: بيانات ناقصة أو تحتاج مراجعة.

---

## 6) API مقترح

### A) جلب تنبيهات رقم الهاتف
```http
GET /api/customer-alerts/phone-groups
```

Query params:
- `q`: بحث برقم الهاتف، اسم العميل، رقم الطلب.
- `severity`: `all | critical | high | medium | low`
- `posStatus`: حالة الطلب في POS.
- `store`: اسم الاستور.
- `ageDays`: الطلبات الأقدم من عدد أيام.
- `limit`

Response مقترح:
```json
{
  "groups": [
    {
      "phone": "9715xxxxxxx",
      "display_phone": "+971 5x xxx xxxx",
      "customer_names": ["Ahmed Ali"],
      "order_count": 3,
      "stored_piece_count": 5,
      "severity": "critical",
      "alerts": [
        "طلب Delivered وما زال موجوداً في الاستور",
        "العميل لديه أكثر من طلب"
      ],
      "orders": [
        {
          "order_no": "256719",
          "customer_name": "Ahmed Ali",
          "pos_status": "Delivered",
          "qty_in_order": 4,
          "qty_in_store": 1,
          "match_state": "missing",
          "order_date": "2026-05-20",
          "delivery_date": "2026-05-22",
          "remark": "Pack: 25,",
          "total_amount": 120,
          "store_slots": [
            { "store": "conveyer", "row": 1, "column": 25 }
          ]
        }
      ]
    }
  ],
  "summary": {
    "total_groups": 20,
    "critical": 2,
    "high": 5,
    "medium": 9,
    "low": 4
  }
}
```

### B) فحص رقم هاتف واحد
```http
POST /api/customer-alerts/check-phone
```

Body:
```json
{
  "phone": "0501234567"
}
```

الاستخدام:
- عند بحث الموظف عن طلب.
- عند فتح POS Connect.
- قبل تأكيد التسليم.

### C) حل أو تأجيل تنبيه
```http
POST /api/customer-alerts/resolve-case
```

Body:
```json
{
  "caseId": 15,
  "action": "resolved",
  "note": "تم تسليم القطعة المتبقية للعميل"
}
```

Actions:
- `resolved`
- `snoozed`
- `ignored`
- `manager_override`

---

## 7) جداول قاعدة البيانات المقترحة

### جدول حالات التنبيه
```sql
customer_alert_cases
```

الحقول:
- `id`
- `case_type`
- `severity`
- `phone_normalized`
- `customer_name`
- `order_nos` JSON
- `status`: `open | resolved | snoozed | ignored`
- `first_detected_at`
- `last_seen_at`
- `resolved_at`
- `resolved_by`
- `note`

### جدول أحداث الحالة
```sql
customer_alert_case_events
```

الحقول:
- `id`
- `case_id`
- `event_type`
- `message`
- `created_by`
- `created_at`

هذا يفيد لمعرفة من تجاهل التنبيه، ومن حله، ومتى ظهر أول مرة.

---

## 8) تصميم الواجهة المقترح

### أعلى التاب
- بحث سريع:
  - رقم هاتف.
  - رقم طلب.
  - اسم العميل.
- فلاتر:
  - الخطورة.
  - حالة POS.
  - الاستور.
  - الطلبات القديمة.
  - فقط `Delivered but still stored`.
- إحصائيات:
  - إجمالي التنبيهات.
  - Critical.
  - High.
  - Delivered وما زال في الاستور.

### جدول التنبيهات
كل صف يمثل رقم هاتف/عميل:
- رقم الهاتف.
- اسم العميل.
- عدد الطلبات.
- عدد القطع الموجودة في الاستور.
- أعلى خطورة.
- آخر تاريخ طلب.
- عدد الطلبات التي حالتها Delivered وما زالت مخزنة.
- زر فتح التفاصيل.

### تفاصيل الصف
عند فتح الصف تظهر كل الطلبات المرتبطة:
- رقم الطلب.
- حالة POS.
- كمية POS.
- كمية الاستور.
- مواقع التخزين.
- remark.
- السعر.
- تاريخ الطلب.
- تاريخ التوصيل.
- أزرار:
  - فتح مواقع الاستور.
  - إعادة التحقق من POS.
  - وضع علامة تم الحل.
  - تأجيل التنبيه.
  - إرسال واتساب للعميل إذا كان الطلب مكتمل فقط.

---

## 9) قواعد أمان مهمة

1. لا يتم إرسال رسالة للعميل تلقائياً عند وجود خطأ داخلي.
   - الخطأ يظهر للموظف فقط.

2. إذا كان الطلب `Delivered` وما زالت له قطع في الاستور:
   - يمنع النظام إخفاء التنبيه إلا بسبب واضح.
   - يفضل طلب صلاحية مدير عند `manager_override`.

3. لا يتم اعتبار الطلب جاهزاً للإرسال إذا:
   - `qty_in_store < qty_in_order`
   - أو توجد طلبات أخرى لنفس الهاتف بحالة خطرة.

4. كل تجاهل أو حل لتنبيه يجب تسجيله في log.

---

## 10) نقاط دمج ذكية مع النظام الحالي

### عند POS Connect
بعد جلب طلب من POS:
- نأخذ رقم الهاتف.
- نفحص هل له طلبات أخرى.
- إذا يوجد خطر، نعرض تنبيه في نفس الصفحة.

### عند Import Numbers from Excel
بعد إدخال أرقام الطلبات للاستور:
- يتم تشغيل فحص رقم الهاتف في الخلفية.
- إذا ظهر طلب Delivered وما زال مخزناً، يظهر في تاب التنبيهات.

### عند نقل طلب إلى استور الكونفير
إذا remark مثل:
```text
Pack: 25,
```
والنظام وضع رقم الطلب في slot 25، يتم فحص نفس رقم الهاتف لمعرفة هل له طلبات أخرى داخل الكونفير أو أي استور آخر.

### عند محاولة التسليم
قبل اعتماد التسليم:
- فحص سريع برقم الهاتف.
- إذا يوجد تنبيه `Critical` أو `High` يظهر modal للموظف.

---

## 11) مراحل التنفيذ

### المرحلة 1: قراءة فقط
- إضافة endpoint `GET /api/customer-alerts/phone-groups`.
- تجميع الطلبات حسب رقم الهاتف.
- عرض تاب جديد داخل صفحة التنبيهات.
- بدون تعديل بيانات أو إرسال رسائل.

### المرحلة 2: إدارة الحالات
- إضافة جدول `customer_alert_cases`.
- دعم `resolved`, `snoozed`, `ignored`.
- إضافة سجل معالجة.

### المرحلة 3: حماية التسليم
- ربط الفحص مع شاشة التسليم أو أي عملية تسليم.
- منع أو تحذير قبل التسليم إذا توجد حالة خطرة.

### المرحلة 4: أتمتة وتنبيهات
- Badge على أيقونة Alerts بعدد الحالات المفتوحة.
- فحص دوري كل فترة.
- إرسال إشعار داخلي للمدير عند `Critical`.

### المرحلة 5: تقارير
- تقرير أسبوعي:
  - كم حالة تم اكتشافها.
  - كم طلب Delivered بقي في الاستور.
  - أكثر الأخطاء تكراراً.
  - الموظفين أو الفروع الأكثر احتياجاً للمراجعة.

---

## 12) معايير القبول
تعتبر الميزة ناجحة إذا:
- يستطيع النظام كشف عميل لديه أكثر من طلب بنفس رقم الهاتف.
- يظهر تنبيه عندما POS يقول `Delivered` لكن رقم الطلب ما زال في الاستور.
- يظهر الفرق بين كمية POS وكمية الاستور.
- يمكن البحث برقم الهاتف أو رقم الطلب.
- تظهر الحالات في تاب منفصل داخل صفحة Alerts.
- يمكن حل أو تأجيل التنبيه مع حفظ السبب.
- لا يتم إرسال رسائل للعميل عند وجود خطأ داخلي إلا بعد قرار الموظف.

---

## 13) خلاصة التنفيذ المقترح
أفضل تصميم هو أن تبقى صفحة Alerts هي مركز التحكم، ونضيف لها تاب جديد اسمه **تنبيهات رقم الهاتف**.

النظام سيقرأ أرقام الطلبات المخزنة، يجلب بياناتها من POS، ينظف أرقام الهواتف، ثم يجمع كل طلبات العميل في مجموعة واحدة. بعد ذلك يحسب المخاطر مثل:
- أكثر من طلب لنفس العميل.
- طلب Delivered وما زال في الاستور.
- نقص أو زيادة في الكمية.
- طلب قديم منسي.

بهذا يصبح النظام مساعداً للموظف قبل التسليم، وليس مجرد شاشة إرسال رسائل.

---

## 14) حالة التنفيذ الحالية
تم بدء تنفيذ المرحلة الأولى:
- إضافة API جديد:
  - `GET /api/customer-alerts/phone-groups`
- إضافة حقول POS المهمة إلى مرشحي التنبيهات:
  - حالة POS.
  - تاريخ الطلب.
  - تاريخ التوصيل.
  - عنوان العميل.
  - remark.
  - رقم الهاتف normalized.
- إضافة تجميع حسب رقم الهاتف.
- إضافة حساب الخطورة:
  - `critical`
  - `high`
  - `medium`
  - `low`
- إضافة تاب جديد داخل صفحة Alerts:
  - **تنبيهات رقم الهاتف**
- إضافة بحث وفلاتر:
  - رقم هاتف.
  - رقم طلب.
  - اسم العميل.
  - الاستور/الموقع.
  - الخطورة.
  - حالة POS.
  - الطلبات القديمة بعد عدد أيام.

المتبقي للمرحلة التالية:
- حفظ الحالات في جدول دائم.
- أزرار حل/تأجيل/تجاهل.
- Badge على أيقونة Alerts.
- ربط الفحص مباشرة قبل اعتماد التسليم.
